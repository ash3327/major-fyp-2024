# test

* **Term 1`
    * image-stream-1: PyTorch, takes in images as input. Uses EfficientNet-B7 as backbone.
    * keypoint-stream-1: TensorFlow, takes in keypoints as input. Uses 5-layer ANN as classifier.
* **Winter**  
  * Problem of previous tests:
    * Normalization (not zero-mean) for image-stream-1.
  * test-2: 
    * 
* **Term 2**